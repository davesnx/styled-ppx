(executables
 (names registry_dump registry_closure_check)
 (libraries
  Css_grammar
  css_spec_parser
  css_grammar_ppx_generate
  ppxlib
  styled_ppx_css_parser))

; Fail if a grammar spec references a missing registry key.

(rule
 (aliases runtest test-css-grammar)
 (deps
  (source_tree ../lib))
 (action
  (run %{exe:registry_closure_check.exe} %{deps})))
