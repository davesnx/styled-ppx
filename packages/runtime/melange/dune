(library
 (name styled_ppx_runtime_melange)
 (public_name styled-ppx.melange)
 (modes melange)
 (wrapped false)
 (flags
  (:standard -nopervasives -open Kloth))
 (libraries reason-react styled-ppx.kloth-melange)
 (preprocess
  (pps melange.ppx -alert -deprecated)))

(copy_files#
 (mode fallback)
 (files ../native/shared/**[!.pp].ml))
