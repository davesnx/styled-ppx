Subtree-escaping selector detection (the guard behind rejecting sibling
interpolations during static extraction) is checked as a cram test.

  $ ../Runner.exe subtree-escape
  ## SubtreeEscape
  - 32 subtree-escape assertions passed
