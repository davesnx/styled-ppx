Regression guard: an OCaml binding name may end in a prime (`inputView'`),
which is a legal, idiomatic identifier character. The class name embeds the
binding label (`css-<content-hash>-<label>`), but `'` is illegal in a CSS
identifier, so emitting it verbatim produced an unmatchable selector
(`.css-<hash>-inputView'`).

The label is sanitized to the CSS-safe set `[A-Za-z0-9_-]`, so the trailing
prime is dropped and the emitted selector is valid CSS.

  $ refmt --parse re --print ml input.re > output.ml
  $ ../../standalone.exe --impl output.ml -o output.ml
  $ styled-ppx.generate output.ml > styles.css

  $ cat styles.css
  /* This file is generated by styled-ppx, do not edit manually */
  .css-12efcmn-inputView{position:absolute}
