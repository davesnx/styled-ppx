(library
 (name murmur2)
 ; Package-private: installed with styled-ppx (the ppx links it) but not
 ; exposed as a findlib library. The hash is an implementation detail of
 ; class-name generation, not public API.
 (package styled-ppx))
