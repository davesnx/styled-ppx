module Color = Css_AtomicTypes.Color

let aliceblue = Color.hex {|F0F8FF|}
let antiquewhite = Color.hex {|FAEBD7|}
let aqua = Color.hex {|00FFFF|}
let aquamarine = Color.hex {|7FFFD4|}
let azure = Color.hex {|F0FFFF|}
let beige = Color.hex {|F5F5DC|}
let bisque = Color.hex {|FFE4C4|}
let black = Color.hex {|000000|}
let blanchedalmond = Color.hex {|FFEBCD|}
let blue = Color.hex {|0000FF|}
let blueviolet = Color.hex {|8A2BE2|}
let brown = Color.hex {|A52A2A|}
let burlywood = Color.hex {|DEB887|}
let cadetblue = Color.hex {|5F9EA0|}
let chartreuse = Color.hex {|7FFF00|}
let chocolate = Color.hex {|D2691E|}
let coral = Color.hex {|FF7F50|}
let cornflowerblue = Color.hex {|6495ED|}
let cornsilk = Color.hex {|FFF8DC|}
let crimson = Color.hex {|DC143C|}
let cyan = Color.hex {|00FFFF|}
let darkblue = Color.hex {|00008B|}
let darkcyan = Color.hex {|008B8B|}
let darkgoldenrod = Color.hex {|B8860B|}
let darkgray = Color.hex {|A9A9A9|}
let darkgreen = Color.hex {|006400|}
let darkgrey = Color.hex {|A9A9A9|}
let darkkhaki = Color.hex {|BDB76B|}
let darkmagenta = Color.hex {|8B008B|}
let darkolivegreen = Color.hex {|556B2F|}
let darkorange = Color.hex {|FF8C00|}
let darkorchid = Color.hex {|9932CC|}
let darkred = Color.hex {|8B0000|}
let darksalmon = Color.hex {|E9967A|}
let darkseagreen = Color.hex {|8FBC8F|}
let darkslateblue = Color.hex {|483D8B|}
let darkslategray = Color.hex {|2F4F4F|}
let darkslategrey = Color.hex {|2F4F4F|}
let darkturquoise = Color.hex {|00CED1|}
let darkviolet = Color.hex {|9400D3|}
let deeppink = Color.hex {|FF1493|}
let deepskyblue = Color.hex {|00BFFF|}
let dimgray = Color.hex {|696969|}
let dimgrey = Color.hex {|696969|}
let dodgerblue = Color.hex {|1E90FF|}
let firebrick = Color.hex {|B22222|}
let floralwhite = Color.hex {|FFFAF0|}
let forestgreen = Color.hex {|228B22|}
let fuchsia = Color.hex {|FF00FF|}
let gainsboro = Color.hex {|DCDCDC|}
let ghostwhite = Color.hex {|F8F8FF|}
let gold = Color.hex {|FFD700|}
let goldenrod = Color.hex {|DAA520|}
let gray = Color.hex {|808080|}
let green = Color.hex {|008000|}
let greenyellow = Color.hex {|ADFF2F|}
let grey = Color.hex {|808080|}
let honeydew = Color.hex {|F0FFF0|}
let hotpink = Color.hex {|FF69B4|}
let indianred = Color.hex {|CD5C5C|}
let indigo = Color.hex {|4B0082|}
let ivory = Color.hex {|FFFFF0|}
let khaki = Color.hex {|F0E68C|}
let lavender = Color.hex {|E6E6FA|}
let lavenderblush = Color.hex {|FFF0F5|}
let lawngreen = Color.hex {|7CFC00|}
let lemonchiffon = Color.hex {|FFFACD|}
let lightblue = Color.hex {|ADD8E6|}
let lightcoral = Color.hex {|F08080|}
let lightcyan = Color.hex {|E0FFFF|}
let lightgoldenrodyellow = Color.hex {|FAFAD2|}
let lightgray = Color.hex {|D3D3D3|}
let lightgreen = Color.hex {|90EE90|}
let lightgrey = Color.hex {|D3D3D3|}
let lightpink = Color.hex {|FFB6C1|}
let lightsalmon = Color.hex {|FFA07A|}
let lightseagreen = Color.hex {|20B2AA|}
let lightskyblue = Color.hex {|87CEFA|}
let lightslategray = Color.hex {|778899|}
let lightslategrey = Color.hex {|778899|}
let lightsteelblue = Color.hex {|B0C4DE|}
let lightyellow = Color.hex {|FFFFE0|}
let lime = Color.hex {|00FF00|}
let limegreen = Color.hex {|32CD32|}
let linen = Color.hex {|FAF0E6|}
let magenta = Color.hex {|FF00FF|}
let maroon = Color.hex {|800000|}
let mediumaquamarine = Color.hex {|66CDAA|}
let mediumblue = Color.hex {|0000CD|}
let mediumorchid = Color.hex {|BA55D3|}
let mediumpurple = Color.hex {|9370DB|}
let mediumseagreen = Color.hex {|3CB371|}
let mediumslateblue = Color.hex {|7B68EE|}
let mediumspringgreen = Color.hex {|00FA9A|}
let mediumturquoise = Color.hex {|48D1CC|}
let mediumvioletred = Color.hex {|C71585|}
let midnightblue = Color.hex {|191970|}
let mintcream = Color.hex {|F5FFFA|}
let mistyrose = Color.hex {|FFE4E1|}
let moccasin = Color.hex {|FFE4B5|}
let navajowhite = Color.hex {|FFDEAD|}
let navy = Color.hex {|000080|}
let oldlace = Color.hex {|FDF5E6|}
let olive = Color.hex {|808000|}
let olivedrab = Color.hex {|6B8E23|}
let orange = Color.hex {|FFA500|}
let orangered = Color.hex {|FF4500|}
let orchid = Color.hex {|DA70D6|}
let palegoldenrod = Color.hex {|EEE8AA|}
let palegreen = Color.hex {|98FB98|}
let paleturquoise = Color.hex {|AFEEEE|}
let palevioletred = Color.hex {|DB7093|}
let papayawhip = Color.hex {|FFEFD5|}
let peachpuff = Color.hex {|FFDAB9|}
let peru = Color.hex {|CD853F|}
let pink = Color.hex {|FFC0CB|}
let plum = Color.hex {|DDA0DD|}
let powderblue = Color.hex {|B0E0E6|}
let purple = Color.hex {|800080|}
let rebeccapurple = Color.hex {|663399|}
let red = Color.hex {|FF0000|}
let rosybrown = Color.hex {|BC8F8F|}
let royalblue = Color.hex {|4169E1|}
let saddlebrown = Color.hex {|8B4513|}
let salmon = Color.hex {|FA8072|}
let sandybrown = Color.hex {|F4A460|}
let seagreen = Color.hex {|2E8B57|}
let seashell = Color.hex {|FFF5EE|}
let sienna = Color.hex {|A0522D|}
let silver = Color.hex {|C0C0C0|}
let skyblue = Color.hex {|87CEEB|}
let slateblue = Color.hex {|6A5ACD|}
let slategray = Color.hex {|708090|}
let slategrey = Color.hex {|708090|}
let snow = Color.hex {|FFFAFA|}
let springgreen = Color.hex {|00FF7F|}
let steelblue = Color.hex {|4682B4|}
let tan = Color.hex {|D2B48C|}
let teal = Color.hex {|008080|}
let thistle = Color.hex {|D8BFD8|}
let tomato = Color.hex {|FF6347|}
let turquoise = Color.hex {|40E0D0|}
let violet = Color.hex {|EE82EE|}
let wheat = Color.hex {|F5DEB3|}
let white = Color.hex {|FFFFFF|}
let whitesmoke = Color.hex {|F5F5F5|}
let yellow = Color.hex {|FFFF00|}
let yellowgreen = Color.hex {|9ACD32|}
let transparent = Color.transparent
