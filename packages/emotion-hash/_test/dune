(include_subdirs unqualified)

(cram
 (alias emotion-hash-tests)
 (deps %{bin:node} %{bin:mine} original.js))

; (rule
; (target
; (dir node_modules))
; (alias runtest)
; (action
; (progn
; (run ln -sfn %{project_root}/../../node_modules %{target})
; (run cp -R %{project_root}/test/e2e/cjs/test/e2e .)
; (run ls .))))

(executable
 (name mine)
 (modules mine)
 (public_name mine)
 (libraries styled-ppx.emotion-hash))
