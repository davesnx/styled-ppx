(library
 (name string_interpolation)
 (public_name styled-ppx.string-interpolation)
 (libraries ppxlib ppxlib.astlib sedlex)
 (preprocess
  (pps sedlex.ppx)))

(env
 (dev
  (flags
   (:standard -warn-error -A-32))))
